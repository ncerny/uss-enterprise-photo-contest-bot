# GitHub Copilot Instructions for USS Enterprise Photo Contest Bot

> **Instruction Source:** Always read `AGENTS.md` first—it's the single source of truth for workflow, commands, and constraints. Use this file only as a lightweight reminder.

## Quick Reminders

- Use `bd` for every task (create, update, close, sync). No markdown TODOs or alternate trackers.
- Keep any AI plan notes inside `history/` while a bead is active.
- Triage "pre-existing" issues immediately: fix small ones, log beads bugs (with repro steps) for larger ones.

For everything else—project overview, tech stack, Firebase/Discord constraints, session protocols—refer back to `AGENTS.md`.
