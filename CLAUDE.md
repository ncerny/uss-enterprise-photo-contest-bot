# Claude Code Instructions for USS Enterprise Photo Contest Bot

> **Instruction Source:** `AGENTS.md` is the canonical guide. Read it before (or alongside) this file so you follow the latest workflow, tooling, and platform constraints.

## Quick Reminders

- Use [bd (beads)](https://github.com/steveyegge/beads) for **all** task tracking—no markdown TODO lists.
- Store any AI planning or scratch notes inside `history/` while working a bead.
- If you encounter a "pre-existing" issue, either fix it immediately (when small) or open a beads bug with repro steps.

Everything else—process, tech stack, platform constraints, and command references—lives in `AGENTS.md`.
